name: Build and Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build-test-pack:
    name: Build, test, and pack
    runs-on: windows-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Show .NET SDK
        run: dotnet --info

      - name: Restore
        run: dotnet restore Nexus.slnx

      - name: Build
        run: dotnet build Nexus.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test Nexus.slnx --configuration Release --no-build -- xunit.parallelizeTestCollections=false

      # ── Pack all production-ready protocol packages ──
      - name: Pack Nexus.Modbus
        run: dotnet pack src\Nexus.Modbus --configuration Release --no-build --output artifacts\packages

      - name: Pack Nexus.Siemens
        run: dotnet pack src\Nexus.Siemens --configuration Release --no-build --output artifacts\packages

      - name: Pack Nexus.Mitsubishi
        run: dotnet pack src\Nexus.Mitsubishi --configuration Release --no-build --output artifacts\packages

      - name: Pack Nexus.Omron
        run: dotnet pack src\Nexus.Omron --configuration Release --no-build --output artifacts\packages

      - name: Pack Nexus.AllenBradley
        run: dotnet pack src\Nexus.AllenBradley --configuration Release --no-build --output artifacts\packages

      - name: Upload NuGet packages
        uses: actions/upload-artifact@v4
        with:
          name: nexus-nupkgs
          path: artifacts/packages/*.nupkg
          if-no-files-found: error
