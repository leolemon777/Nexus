<Page x:Class="Nexus.App.Views.SimulatorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource Brush.Bg}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="12">
        <StackPanel>

            <!-- 模拟器控制 -->
            <Border Style="{DynamicResource Card}" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Modbus TCP Server" Style="{DynamicResource Label}" Margin="0,0,0,8"/>

                    <WrapPanel Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="地址" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Style="{DynamicResource Input}" Width="160" Margin="0,0,8,0"
                                     Text="{Binding Host, Mode=OneWay}" IsReadOnly="True" Opacity="0.7"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="端口" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Style="{DynamicResource Input}" Width="110"
                                     Text="{Binding Port, UpdateSourceTrigger=LostFocus}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Button Content="▶ 启动" Style="{DynamicResource Button.Primary}"
                                    MinWidth="140" Margin="0,0,8,0" Command="{Binding StartCommand}"/>
                            <Button Content="■ 停止" Style="{DynamicResource Button.Danger}"
                                    MinWidth="140" Command="{Binding StopCommand}"/>
                        </StackPanel>
                    </WrapPanel>

                    <!-- 状态指示 -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Ellipse Width="12" Height="12" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource Brush.Text3}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource Brush.Ok}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Grid.Column="1" Text="{Binding Status}" Style="{DynamicResource Body}"
                                   FontWeight="Bold" Margin="8,0,24,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Style="{DynamicResource Body}" VerticalAlignment="Center">
                            <Run Text="连接数: "/><Run Text="{Binding ConnectionCount, Mode=OneWay}" FontWeight="Bold"/>
                        </TextBlock>
                    </Grid>

                    <!-- 延迟模拟 -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="模拟延迟" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <Slider Grid.Column="1" Minimum="0" Maximum="2000" Value="{Binding SimulatedLatencyMs, Mode=TwoWay}"
                                TickFrequency="100" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="8,0,0,0">
                            <Run Text="{Binding SimulatedLatencyMs, Mode=OneWay}"/><Run Text=" ms"/>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 内存编辑 -->
            <Border Style="{DynamicResource Card}" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="内存区" Style="{DynamicResource Label}" Margin="0,0,0,8"/>

                    <WrapPanel Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="区域" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox Width="180" ItemsSource="{Binding MemoryAreas}" SelectedItem="{Binding SelectedMemoryArea, Mode=TwoWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="起始" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Style="{DynamicResource Input}" Width="120" Text="{Binding MemoryStartAddress, UpdateSourceTrigger=LostFocus}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="数量" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Style="{DynamicResource Input}" Width="100" Text="{Binding MemoryCount, UpdateSourceTrigger=LostFocus}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,12,4">
                            <TextBlock Text="{Binding PresetInfo}" Style="{DynamicResource Body}" Foreground="{DynamicResource Brush.Text3}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Button Content="刷新" Style="{DynamicResource Button.Ghost}" Margin="0,0,8,0" Command="{Binding RefreshMemoryCommand}"/>
                            <Button Content="重置" Style="{DynamicResource Button.Ghost}" Command="{Binding ResetDataCommand}"/>
                        </StackPanel>
                    </WrapPanel>

                    <WrapPanel Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="地址" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Style="{DynamicResource Input}" Width="140" Text="{Binding EditAddress, UpdateSourceTrigger=LostFocus}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,4">
                            <TextBlock Text="值" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Style="{DynamicResource Input}" Width="160" Text="{Binding EditValue, UpdateSourceTrigger=LostFocus}"/>
                        </StackPanel>
                        <Button Content="写入内存" Style="{DynamicResource Button.Primary}" MinWidth="140" Margin="0,0,0,4" Command="{Binding ApplyMemoryValueCommand}"/>
                    </WrapPanel>

                    <DataGrid ItemsSource="{Binding MemoryRows}" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeaderWidth="0"
                              CanUserAddRows="False" CanUserDeleteRows="False" Height="220">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="区域" Binding="{Binding Area}" Width="*"/>
                            <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="160"/>
                            <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="210"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- 日志 -->
            <Border Style="{DynamicResource Card}" Margin="0,0,0,8">
                <StackPanel>
                    <Grid Margin="0,0,0,6">
                        <TextBlock Text="模拟器日志" Style="{DynamicResource Label}" VerticalAlignment="Center"/>
                        <Button Content="清空" Style="{DynamicResource Button.Text}" HorizontalAlignment="Right"
                                Command="{Binding ClearLogCommand}"/>
                    </Grid>
                    <Border Background="{DynamicResource Brush.Bg}" CornerRadius="{DynamicResource Radius.Md}" Padding="12" Height="180">
                        <ListBox x:Name="LogListBox" Background="Transparent" BorderThickness="0"
                                 Foreground="{DynamicResource Brush.Text2}"
                                 FontFamily="Consolas, Courier New" FontSize="12"
                                 ItemsSource="{Binding LogLines}"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
