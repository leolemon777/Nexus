<Page x:Class="Nexus.App.Views.FujiPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource Brush.Bg}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="12">
        <StackPanel>

            <!-- 连接配置 -->
            <Border Style="{DynamicResource Card}" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="连接配置" Style="{DynamicResource Label}" Margin="0,0,0,6"/>
                    <WrapPanel Margin="0,0,0,8">
                        <TextBlock Text="串口" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Style="{DynamicResource Input}" Width="140" Margin="0,0,16,0" Text="{Binding ComPort, UpdateSourceTrigger=LostFocus}"/>
                        <TextBlock Text="波特率" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="140" ItemsSource="{Binding BaudRates}" SelectedItem="{Binding BaudRate, Mode=TwoWay}" Margin="0,0,16,0"/>
                        <TextBlock Text="站号" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Style="{DynamicResource Input}" Width="84" Text="{Binding Station, UpdateSourceTrigger=LostFocus}"/>
                    </WrapPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="连接" Style="{DynamicResource Button.Primary}" Margin="12,0,0,0" Command="{Binding ConnectCommand}"/>
                        <Button Grid.Column="1" Content="断开" Style="{DynamicResource Button.Ghost}" Margin="8,0,0,0" Command="{Binding DisconnectCmdCommand}"/>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource Brush.Text3}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource Brush.Ok}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding ConnectionStatus}" Style="{DynamicResource Body}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 数据读写 -->
            <Border Style="{DynamicResource Card}" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="数据读写" Style="{DynamicResource Label}" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="地址" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" Style="{DynamicResource Input}" Text="{Binding Address, UpdateSourceTrigger=LostFocus}" ToolTip="{Binding AddressHint}"/>
                        <TextBlock Grid.Column="2" Text="类型" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="16,0,8,0"/>
                        <ComboBox Grid.Column="3" MinWidth="200" ItemsSource="{Binding DataTypes}" SelectedItem="{Binding DataType, Mode=TwoWay}"/>
                    </Grid>

                    <!-- 读取 -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="读取" FontWeight="Bold" Foreground="{DynamicResource Brush.Accent}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <Button Grid.Column="1" Content="读取" Style="{DynamicResource Button.Primary}" MinWidth="140" Command="{Binding ReadCommand}"/>
                        <TextBlock Grid.Column="2" Text="结果" Style="{DynamicResource Body}" VerticalAlignment="Center" Margin="24,0,8,0"/>
                        <Border Grid.Column="3" Background="{DynamicResource Brush.Bg}" CornerRadius="{DynamicResource Radius.Md}" Padding="10,4" BorderBrush="{DynamicResource Brush.Line}" BorderThickness="1" MinHeight="32">
                            <TextBlock Text="{Binding LastResult}" Style="{DynamicResource Readout}" FontSize="16" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>

                    <!-- 写入 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="写入" FontWeight="Bold" Foreground="{DynamicResource Brush.Warn}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <TextBox Grid.Column="1" Style="{DynamicResource Input}" Text="{Binding WriteValue, UpdateSourceTrigger=LostFocus}"/>
                        <Button Grid.Column="2" Content="写入" Style="{DynamicResource Button.Danger}" MinWidth="140" Margin="24,0,0,0" Command="{Binding WriteCommand}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 通讯日志 -->
            <Border Style="{DynamicResource Card}" Margin="0,0,0,8">
                <StackPanel>
                    <Grid Margin="0,0,0,6">
                        <TextBlock Text="通讯日志" Style="{DynamicResource Label}" VerticalAlignment="Center"/>
                        <Button Content="清空" Style="{DynamicResource Button.Text}" HorizontalAlignment="Right" Command="{Binding ClearLogCommand}"/>
                    </Grid>
                    <Border Background="{DynamicResource Brush.Bg}" CornerRadius="{DynamicResource Radius.Md}" Padding="12" Height="220">
                        <ListBox x:Name="LogListBox" Background="Transparent" BorderThickness="0"
                                 Foreground="{DynamicResource Brush.Text2}"
                                 FontFamily="Consolas, Courier New" FontSize="12"
                                 ItemsSource="{Binding LogLines}"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
