<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">
  <!-- Form style: hud  (若 .NET 报 mscorlib 错，把上面 assembly=System.Runtime 改为 mscorlib) -->
  <CornerRadius x:Key="Radius.Lg">2</CornerRadius>
  <CornerRadius x:Key="Radius.Md">2</CornerRadius>
  <CornerRadius x:Key="Radius.Sm">2</CornerRadius>
  <Thickness x:Key="Pad.Card">22</Thickness>
  <Thickness x:Key="Border.Thickness">1</Thickness>
  <FontFamily x:Key="Font.Family">Space Grotesk, Microsoft YaHei UI, Segoe UI</FontFamily>
  <sys:Double x:Key="Font.Size">13</sys:Double>
  <sys:Double x:Key="Font.SizeH">18</sys:Double>
  <sys:Double x:Key="Font.SizeHero">30</sys:Double>
  <sys:Double x:Key="Font.SizeReadout">42</sys:Double>
  <DropShadowEffect x:Key="CardEffect" Opacity="0"/>

  <Style x:Key="Card" TargetType="Border">
    <Setter Property="Background">
      <Setter.Value>
        <SolidColorBrush Color="{DynamicResource Color.Surface2}" Opacity="0.78"/>
      </Setter.Value>
    </Setter>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.AccentLine}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="{DynamicResource Radius.Lg}"/>
    <Setter Property="Padding" Value="{DynamicResource Pad.Card}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
  </Style>

  <Style x:Key="Button.Primary" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource Brush.OnAccent}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Padding" Value="18,11"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Grid>
            <Path x:Name="glow" Stretch="Fill" Data="M 0.10,0 L 1,0 L 1,0.75 L 0.90,1 L 0,1 L 0,0.25 Z"
                  Fill="{DynamicResource Brush.AccentSoft}" Opacity="0">
              <Path.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.70" Color="{DynamicResource Color.Accent}"/>
              </Path.Effect>
            </Path>
            <Path x:Name="shape" Stretch="Fill" Data="M 0.10,0 L 1,0 L 1,0.75 L 0.90,1 L 0,1 L 0,0.25 Z"
                  Fill="{DynamicResource Brush.AccentGrad}" Stroke="{DynamicResource Brush.AccentLine}" StrokeThickness="1"/>
            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="glow" Property="Opacity" Value="0.85"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="shape" Property="Opacity" Value="0.78"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="shape" Property="Opacity" Value="0.36"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Button.Ghost" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Padding" Value="18,11"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Grid>
            <Path x:Name="glow" Stretch="Fill" Data="M 0.10,0 L 1,0 L 1,0.75 L 0.90,1 L 0,1 L 0,0.25 Z"
                  Fill="{DynamicResource Brush.AccentSoft}" Opacity="0"/>
            <Path x:Name="shape" Stretch="Fill" Data="M 0.10,0 L 1,0 L 1,0.75 L 0.90,1 L 0,1 L 0,0.25 Z"
                  Fill="{DynamicResource Brush.Surface2}" Stroke="{DynamicResource Brush.AccentLine}" StrokeThickness="1"/>
            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="glow" Property="Opacity" Value="0.65"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="shape" Property="Fill" Value="{DynamicResource Brush.AccentSoft}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
