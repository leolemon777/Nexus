<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
  <!-- Form style: material  (若 .NET 报 mscorlib 错，把上面 assembly=mscorlib 改为 System.Runtime) -->
  <CornerRadius x:Key="Radius.Lg">16</CornerRadius>
  <CornerRadius x:Key="Radius.Md">999</CornerRadius>
  <CornerRadius x:Key="Radius.Sm">8</CornerRadius>
  <Thickness x:Key="Pad.Card">22</Thickness>
  <Thickness x:Key="Border.Thickness">0</Thickness>
  <FontFamily x:Key="Font.Family">Roboto, Microsoft YaHei UI, Manrope, Segoe UI</FontFamily>
  <sys:Double x:Key="Font.Size">14</sys:Double>
  <sys:Double x:Key="Font.SizeH">20</sys:Double>
  <sys:Double x:Key="Font.SizeHero">32</sys:Double>
  <sys:Double x:Key="Font.SizeReadout">44</sys:Double>
  <DropShadowEffect x:Key="CardEffect" BlurRadius="22" ShadowDepth="6" Direction="270" Opacity="0.30" Color="Black"/>

  <Style x:Key="Card" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource Brush.Surface2}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Line}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="CornerRadius" Value="{DynamicResource Radius.Lg}"/>
    <Setter Property="Padding" Value="{DynamicResource Pad.Card}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
  </Style>

  <Style x:Key="Button.Primary" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource Brush.OnAccent}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Padding" Value="18,11"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="clip" CornerRadius="{DynamicResource Radius.Md}" ClipToBounds="True">
            <Grid>
              <Border x:Name="b" Background="{DynamicResource Brush.Accent}" CornerRadius="{DynamicResource Radius.Md}"/>
              <Ellipse x:Name="ripple" Fill="{DynamicResource Brush.OnAccent}" Opacity="0"
                       Width="90" Height="90" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="ripple" Property="Opacity" Value="0.10"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="ripple" Property="Opacity" Value="0.22"/>
              <Setter TargetName="clip" Property="Margin" Value="0,1,0,-1"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="clip" Property="Opacity" Value="0.38"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Button.Ghost" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Padding" Value="18,11"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="clip" CornerRadius="{DynamicResource Radius.Md}" ClipToBounds="True">
            <Grid>
              <Border x:Name="b" Background="{DynamicResource Brush.Surface2}"
                      CornerRadius="{DynamicResource Radius.Md}"/>
              <Ellipse x:Name="ripple" Fill="{DynamicResource Brush.Accent}" Opacity="0"
                       Width="90" Height="90" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="ripple" Property="Opacity" Value="0.08"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="ripple" Property="Opacity" Value="0.18"/>
              <Setter TargetName="clip" Property="Margin" Value="0,1,0,-1"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
