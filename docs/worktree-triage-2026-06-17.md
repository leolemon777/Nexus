# Worktree Triage - 2026-06-17

Purpose: keep the verified cleanup separate from the large experimental scaffold batch so future commits and PRs stay reviewable.

## Current Buckets

### Bucket A: verified review cleanup

This bucket is suitable for a small review-focused PR.

- `.github/workflows/ci.yml`: runs build/test on Windows and uploads packed artifacts without automatic NuGet push.
- `README.md`: replaces inflated protocol-count claims with support tiers.
- `implementation-notes.md`: records cleanup decisions, verification, and risks.
- `docs/worktree-triage-2026-06-17.md`: this triage boundary note.

Mixed files need care before staging because they also contain experimental scaffold wiring:

- `Nexus.slnx`
- `src/Nexus.App/Nexus.App.csproj`
- `src/Nexus.App/App.xaml.cs`
- `src/Nexus.App/ViewModels/MainViewModel.cs`

Do not stage these whole files for Bucket A unless the experimental scaffold work is intentionally included in the same PR.

### Bucket B: experimental protocol and UI scaffolds

Current untracked scale:

- 85 untracked `src/Nexus.*` source project directories
- 111 untracked WPF `ViewModel` files
- 222 untracked WPF page/code-behind files
- 7 untracked test project directories
- 425 scaffold entries in the four groups above

This bucket should be either deleted, moved to an experiment branch, or promoted one protocol family at a time. Before any protocol is promoted as supported, it needs protocol-specific frames/parsers, address semantics, tests, docs, and virtual-server or real-device validation evidence.

### Bucket C: independent tracked changes

These are not part of the review-cleanup boundary and should be reviewed separately:

- `src/Nexus.Core/HeartbeatGuard.cs`: handles `ObjectDisposedException` around heartbeat callback locking.
- `src/Nexus.Omron/OmronHostLinkClient.cs`: adjusts HostLink/FINS response offset comments.

### Bucket D: local config and planning

- `NuGet.Config`: untracked, contains only the public nuget.org source.
- `NEXUS_COMPLETION_PLAN.md`: untracked planning draft. It should not claim scaffold count as production protocol support.
- `docs/worktree-triage-2026-06-17.md`: this new untracked triage note.

## Cleaned

- Deleted the accidental `$null` file generated by PowerShell output redirection.
- Reverted no-op formatting differences in Modbus RTU/ASCII page files so they do not pollute the cleanup review.

## Verified Baseline

- `dotnet build Nexus.slnx --configuration Release --no-restore -m:1`: passed, 0 errors, 10 warnings.
- The 10 warnings are from experimental WPF tool ViewModels with unused or unassigned private fields: `SecurityViewModel`, `WebViewModel`, `GatewayViewModel`, `DataCollectorViewModel`, and `HistoryViewModel`.
- Earlier solution test verification: `dotnet test Nexus.slnx --configuration Release --no-build --verbosity minimal` passed with 0 failures.

## Recommended Next Split

1. Commit Bucket A documentation/CI/readme cleanup first.
2. Decide whether Bucket B should remain in the main worktree, move to a dedicated experiment branch, or be removed.
3. Review Bucket C independently with focused tests.
4. Only promote experimental protocols after they pass the evidence gates above.

## Current Staging State

- Bucket A is staged as of 2026-06-17: `.github/workflows/ci.yml`, `README.md`, `implementation-notes.md`, and this triage note.
- Mixed-boundary files, independent tracked changes, `NuGet.Config`, `NEXUS_COMPLETION_PLAN.md`, and all experimental scaffolds remain unstaged.
- Latest verification after staging: `dotnet build Nexus.slnx --configuration Release --no-restore -m:1` passed with 0 errors and 0 warnings.
